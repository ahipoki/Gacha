#include <iostream>
#include <cstring>
#include "Unit.h"

Unit::Unit(char* newName, int newHP, int newATK)
{
  name = new char[strlen(newName)+1];
  strcpy(name, newName);
  HP = newHP;
  ATK = newATK;
}
